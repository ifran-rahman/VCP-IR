# Cloud Cover Evaluation

Cloud cover estimation experiments using Otsu thresholding on the SWIMSEG dataset.

## Structure

- `src/` : reusable processing functions
- `scripts/` : experiment entry points
- `outputs/` : generated CSV results
- `notebooks/` : analysis and visualization

---

## Installation

```bash
pip install -r requirements.txt